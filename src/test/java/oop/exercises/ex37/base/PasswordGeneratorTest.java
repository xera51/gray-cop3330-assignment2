package oop.exercises.ex37.base;

import static org.junit.jupiter.api.Assertions.*;

class PasswordGeneratorTest {
    // only have a constructor, a random string generator, and getters/setters
    // do not know what to test (everything else is private)
}